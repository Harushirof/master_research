# MATLAB auto-generated
*.asv
*.autosave
*.m~
*.mex*

# MATLAB binary data
*.mat
*.fig
*.slx

# Experimental data (large files — track with external storage if needed)
*.csv
*.bin
*.zip
*.MEM

# Audio recordings (very large — keep in external storage)
*.wav
*.WAV

# Output files (regenerable)
*.pdf
*.svg
*.bmp
*.mdd
# NOTE: *.png は追跡する（発表図・解析図を残すため, 2026-06-14〜。全PNG追跡で統一）

# OS
.DS_Store
Thumbs.db

# Tooling (local settings, not for repo)
.claude/
node_modules/

# Office temp/lock files
~$*
*.tmp
